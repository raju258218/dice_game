// usecase 1
let position=[0, 0]; 
let steps=[0, 0];        
let turn=0;              
  // usecase 2
  let dice=Math.floor(Math.random()* 6)+1;
  steps[turn]++;
  console.log(" Player " +Number(turn+1) +" rolled:" +dice);